#pragma once

// Generated by NewClass.py — edit as needed.

#include <cstddef>
#include <cstdint>
#include <lumina/core/NoCopyable.h>
#include <lumina/io/FileReader.h>
#include <lumina/io/FileWriter.h>

namespace lumina::extensions { inline namespace experimental {
class CkptManager : public core::NoCopyable
{
public:
    CkptManager() = default;
    virtual ~CkptManager() = default;

    // TODO: add ckpt header or not.
    // TODO: add public API.
    virtual std::unique_ptr<io::FileWriter> CreateCkptFileWriter() = 0;

    // If this function return false, there is no file reader
    // If this function return true, there
    virtual core::Result<bool> HasCkptFile() = 0;

    // This function is designed based on the following contract: it will always be called after HasCkptFile.
    // If HasCkptFile returns true, it guarantees a valid pointer.
    // Otherwise, it returns a nullptr to indicate an error.
    virtual std::unique_ptr<io::FileReader> GetCkptFileReader() = 0;
};
}} // namespace lumina::extensions::experimental
